Fix three layout issues in the cart drawer for AA WIGS. Reference screenshot attached — here’s exactly where each fix goes:
1. Product thumbnail per color
Add a thumbnail image (roughly 80x80px) to the left of the product info block, in the row that currently shows “AA Signature Body Wave / 1B Natural Black · 22” / $379.00.” Position it so the product name, variant text, and price shift right to make room — thumbnail on the far left of that row, vertically centered against the product name and quantity selector (the +/- stepper stays on the right, unchanged). The thumbnail must match whichever color variant is selected (e.g., if the customer picked 1B Natural Black, show that color’s image). Wire this to the existing color/variant data — I’ll supply one image per color; use a placeholder if a color’s image is missing so nothing breaks.
There is currently a large empty gap between the price row and the ivory subtotal section below it — once the thumbnail is added, that gap should shrink to normal spacing, not stay as dead space.
2. Hide the AA Concierge bubble while the cart drawer is open
The Concierge bubble currently sits at the bottom-right of the screen, directly overlapping the bottom-right corner of the “14-Day Returns” box and crowding the top-right of the “CHECKOUT — $379.00” button. Hide this bubble completely any time the cart drawer is open — don’t reposition it, remove it from view entirely — and restore it once the drawer is closed. Nothing should render in that bottom-right zone while checkout is visible.
3. Fix the cut-off reassurance row
Below “Continue Shopping” and above “THE FIRST CIRCLE” link, there should be a row showing “Free U.S. Shipping,” “Secure Stripe Checkout,” and “Signature Packaging Included” — right now this is missing/clipped in the drawer. Add it as a fully contained row between “Continue Shopping” and “THE FIRST CIRCLE,” inside the drawer’s own scroll area, sized to fit on an iPhone SE-width screen without any text wrapping off-screen or being cut at the edges. If all three don’t fit on one line at the smallest supported width, stack them vertically rather than truncating.
After these three fixes, send me a screenshot of the cart drawer on both a small mobile viewport (iPhone SE) and a standard viewport (iPhone 14/15) so I can confirm spacing before anything else changes.